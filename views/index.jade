extends layout

block content
  h1= title
  p Welcome to #{title}
  if user
      p 当前用户：#{user.username}
          a.btn.btn-link(href='/logout') 退 出
  else
      a.btn.btn-link(href='/login') 登 录
      a.btn.btn-link(href='/register') 注 册
  br
  a.btn.btn-link(href='/topics') 文章列表